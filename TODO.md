# TODO
- [ ] Refactor/move the existing `CollapsibleSidebar` from `FentonChart.tsx` into a shared component (so the form card in `growchart.tsx` can reuse it).
- [ ] Update `Client/src/Components/Growchart/growchart.tsx` to wrap the left `formCard` (Patient Info + Visits) in the minimizable sidebar.
- [ ] Ensure the chart area resizes/redraws properly after toggle.
- [ ] Verify styling/UX: rail button, smooth GSAP animation, no content overflow.
- [ ] Run frontend checks (npm test/build/lint) and do manual verification.

